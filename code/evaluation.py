import os
import pickle

apps = os.listdir('evaluations')
for app in apps:
    # Load Ground Truth

    # Load Predictions

    # Treat as Multi-class classification problem
    for pattern in groundtruth:
